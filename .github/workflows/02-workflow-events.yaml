name: 02-workfloe-events

on: 
  push:
  pull_request:
  schedule:
    - cron: '*/5 * * * *' # every day at midnight

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
          - name: Show the trigger
            run: echo "i have been triggered by ${{ github.event_name }}!"